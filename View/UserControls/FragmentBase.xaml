<UserControl x:Class="View.UserControls.FragmentBase"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:View.UserControls"
             mc:Ignorable="d">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"></ColumnDefinition>
            <ColumnDefinition Width="*" MinWidth="128"></ColumnDefinition>
            <ColumnDefinition Width="*" MinWidth="128"></ColumnDefinition>
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Column="0" TextTrimming="CharacterEllipsis"
                   HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,0,8,0">
            Value:
        </TextBlock>
        <TextBox Grid.Column="1" VerticalContentAlignment="Center">
            100e-2
        </TextBox>
        <TextBlock Grid.Column="2" VerticalAlignment="Center" Margin="8,0,0,0">
            HEX: D1 17 A1 A1
        </TextBlock>
    </Grid>
</UserControl>